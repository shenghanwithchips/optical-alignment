# Optical Alignment Review Template

Use this template when reviewing a screenshot, Figma frame, component, CSS implementation, landing page, poster, or UI detail.

## Scope

Component or area reviewed: [name]
Medium: [Figma / browser / native app / print / screenshot]
Actual size checked: [yes/no]
Real content checked: [yes/no]

## Mechanical baseline

Grid/margin/centerline/bounding box/baseline currently used:

Current values if known:

## Optical symptom

What looks visually off:

Direction of perceived pull or imbalance:

Where it is most visible:

## Perceptual cause

Likely cause:

Relevant factors: typeface, weight, size, line break, punctuation, baseline, descender, all-caps/lowercase, icon silhouette, component padding, SVG viewBox, browser rendering.

## Recommended nudge

Correction:

Approximate magnitude:

Why this nudge is small enough and axis-specific:

## Implementation note

Figma/component/CSS/code change:

Scope: component-wide / breakpoint-specific / fixed copy only / one-off.

Comment or token name to use:

## Validation

Before/after comparison method:

Breakpoints/devices/states to check:

Accessibility checks:

Retest if these change:
